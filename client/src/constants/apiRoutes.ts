const authPrefix = '/auth';
const postsPrefix = '/posts';

// auth
export const LOGIN_API = `${authPrefix}/login`;
export const REFRESH_API = `${authPrefix}/refresh`;

// posts
export const GET_ALL_POSTS_API = `${postsPrefix}/get-all`;
export const GET_POST_API = `${postsPrefix}/get`;
